spring.application.name=ecommerce
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ecommerce_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:123456}

# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate / JPA para producción
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true


jwt.secret=${JWT_SECRET:L00+6yBWmlrdTjAHjnPn6OO+MEvXHODJLe7FZCuSYMU=}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:oyarlgdq}
cloudinary.api-key=${CLOUDINARY_API_KEY:258867321551173}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:ia_235D6SEZh-jhLZrysHNjD6aw}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# Configuración esencial de HikariCP para Supabase (esto déjalo fijo en el properties)
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=30000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false